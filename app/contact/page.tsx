import Template from "@/components/default/template";

export default function Home() {
	return (
		<Template>
			<>
				{
					// TODO: Update the content of the page, and if need be change what the page is meant to be.
				}
			</>
		</Template>
	);
}
